
const APP = {"phases": [{"id": "fase-1", "title": "Fase 1 · Imagen digital básica", "summary": "Conceptos básicos de imagen digital y práctica guiada."}, {"id": "fase-2", "title": "Fase 2 · Tratamiento de imágenes", "summary": "Resolución, formatos y ajustes básicos."}, {"id": "fase-3", "title": "Fase 3 · Creación de carteles", "summary": "Prácticas con GIMP y Photopea, con guía paso a paso."}, {"id": "fase-4", "title": "Fase 4 · Vídeo y audio digital", "summary": "Vídeo publicitario y podcast publicitario con OpenShot y Audacity."}, {"id": "fase-5", "title": "Fase 5 · Mini vídeo mejorado", "summary": "Mejora del vídeo de la fase anterior reutilizando la misma marca."}], "tests": {"fase-1": [{"q": "¿Qué unidad se usa normalmente para medir el tamaño de una imagen en pantalla?", "options": ["Píxeles", "Vatios", "Hercios", "Megabytes por segundo"], "answer": 0}, {"q": "¿Qué formato suele usar compresión con pérdida para fotografías?", "options": ["PNG", "JPG/JPEG", "SVG", "TXT"], "answer": 1}, {"q": "Una imagen con más resolución suele tener...", "options": ["menos detalle", "más detalle", "menos color", "menos tamaño siempre"], "answer": 1}, {"q": "¿Qué significa editar una imagen?", "options": ["Imprimirla", "Modificar su contenido o apariencia", "Solo guardarla", "Copiarla a un USB"], "answer": 1}, {"q": "¿Qué formato conserva transparencia de forma habitual?", "options": ["PNG", "JPG", "MP3", "DOCX"], "answer": 0}], "fase-2": [{"q": "La resolución de una imagen se refiere a...", "options": ["Su precio", "La cantidad de detalle o píxeles", "El volumen", "La duración"], "answer": 1}, {"q": "Si una imagen se amplía demasiado puede verse...", "options": ["más nítida", "pixelada", "más rápida", "estéreo"], "answer": 1}, {"q": "¿Qué formato es adecuado para logotipos con fondo transparente?", "options": ["PNG", "MP4", "WAV", "JPG"], "answer": 0}, {"q": "Brillo y contraste sirven para...", "options": ["Cambiar el sonido", "Ajustar luminosidad y diferencia entre tonos", "Acelerar el vídeo", "Comprimir un archivo ZIP"], "answer": 1}, {"q": "Recortar una imagen significa...", "options": ["Añadir música", "Eliminar una parte sobrante", "Cambiar el idioma", "Subirla a internet"], "answer": 1}], "fase-3": [{"q": "¿Qué programa puede usarse online sin instalar?", "options": ["GIMP", "Photopea", "Audacity", "OpenShot"], "answer": 1}, {"q": "En GIMP, para añadir texto normalmente se usa la herramienta...", "options": ["Rutas", "Texto", "Difuminar", "Clonar"], "answer": 1}, {"q": "¿Qué opción permite guardar el trabajo final en JPG o PNG?", "options": ["Exportar", "Deshacer", "Seleccionar", "Escalar"], "answer": 0}, {"q": "¿Para un cartel publicitario conviene que el mensaje principal sea...", "options": ["pequeño y escondido", "claro y visible", "solo en inglés", "sin imágenes"], "answer": 1}, {"q": "¿Qué elemento ayuda a identificar una marca en un cartel?", "options": ["Logotipo", "Cursor", "Regla", "Historial"], "answer": 0}], "fase-4": [{"q": "OpenShot es una aplicación para...", "options": ["editar vídeo", "editar hojas de cálculo", "navegar por internet", "crear bases de datos"], "answer": 0}, {"q": "Audacity se usa principalmente para...", "options": ["editar audio", "retocar fotos", "programar", "comprimir carpetas"], "answer": 0}, {"q": "En un vídeo publicitario corto conviene...", "options": ["superar 10 minutos", "usar mensaje claro y breve", "no incluir marca", "quitar el sonido siempre"], "answer": 1}, {"q": "¿Qué formato de exportación de vídeo es habitual para entregar la práctica?", "options": ["MP4", "PSD", "XCF", "ZIP"], "answer": 0}, {"q": "En un podcast publicitario es importante...", "options": ["que no se entienda la voz", "equilibrar voz y música", "eliminar toda pausa", "usar solo ruidos"], "answer": 1}], "fase-5": [{"q": "La fase 5 reutiliza principalmente...", "options": ["la marca y materiales de la fase 4", "solo capturas del escritorio", "una hoja de cálculo", "un documento PDF"], "answer": 0}, {"q": "Una transición en vídeo sirve para...", "options": ["pasar de un clip a otro", "subir el volumen del móvil", "guardar el archivo", "escribir texto fuera del vídeo"], "answer": 0}, {"q": "Un título final puede ayudar a...", "options": ["cerrar el anuncio con llamada a la acción", "borrar el vídeo", "reducir la resolución", "ocultar la marca"], "answer": 0}, {"q": "Mejorar la mezcla de audio significa...", "options": ["equilibrar voz, música y efectos", "quitar todos los clips", "solo cambiar el color", "poner el volumen al máximo siempre"], "answer": 0}, {"q": "Un mini vídeo mejorado debe mantener...", "options": ["la coherencia de marca", "un error de exportación", "las pistas vacías", "imágenes borrosas"], "answer": 0}]}};
const PASS_SCORE = 4;
const MAX_ATTEMPTS = 2;
const CURRENT_KEY = "ra6_videojuego_progress_v5";
const LEGACY_KEYS = ["ra6_videojuego_progress_v4","ra6_videojuego_progress_v3","ra6_videojuego_progress_v2","ra6_videojuego_progress_v1"];
const TEACHER_USER = "tmilgar";
function normalizeText(v){ return String(v||"").trim().toLowerCase(); }
function getTodayCode(){ const d = new Date(); return String(d.getMonth()+1).padStart(2,"0")+String(d.getDate()).padStart(2,"0")+String(d.getFullYear()).slice(-2); }
function initialState(){ return {studentName:"",teacherMode:false,verified:{},passed:{},completed:{},attempts:{},best:{},percent:{},results:{},phase4Brand:""}; }
function migrateLegacy(old){ const s=initialState(); if(!old||typeof old!=="object") return s; Object.assign(s, old); s.studentName = old.studentName||""; s.teacherMode = normalizeText(s.studentName)===TEACHER_USER || !!old.teacherMode; s.verified=old.verified||{}; s.passed=old.passed||{}; s.attempts=old.attempts||{}; s.best=old.best||{}; s.percent=old.percent||{}; s.results=old.results||{}; s.completed = old.completed||{}; for(const ph of APP.phases){ if(old.passed && old.passed[ph.id]) s.completed[ph.id]=true; } return s; }
function loadState(){ let raw=localStorage.getItem(CURRENT_KEY); if(raw){ try{return Object.assign(initialState(), JSON.parse(raw));}catch(e){} } for(const key of LEGACY_KEYS){ raw=localStorage.getItem(key); if(raw){ try{ const s=migrateLegacy(JSON.parse(raw)); localStorage.setItem(CURRENT_KEY, JSON.stringify(s)); return s; }catch(e){} } } return initialState(); }
function saveState(s){ localStorage.setItem(CURRENT_KEY, JSON.stringify(s)); }
function isTeacherMode(s){ return !!s.teacherMode || normalizeText(s.studentName)===TEACHER_USER; }
function phaseIndex(id){ return APP.phases.findIndex(p=>p.id===id); }
function isAccessible(id,s){ if(isTeacherMode(s)) return true; const idx=phaseIndex(id); if(idx<=0) return true; return !!s.completed[APP.phases[idx-1].id]; }
function allCompleted(s){ return APP.phases.every(p=>s.completed[p.id]) || isTeacherMode(s); }
function renderShell(pageId){ const s=loadState(); const progress=APP.phases.filter(p=>s.completed[p.id]).length; const pct=Math.round((progress/APP.phases.length)*100); const bar=document.getElementById("global-progress"); if(bar) bar.style.width=pct+"%"; const txt=document.getElementById("progress-text"); if(txt) txt.textContent=isTeacherMode(s)?"Modo docente activo":`${progress}/${APP.phases.length} fases completadas`; const name=document.getElementById("student-name"); if(name) name.value=s.studentName||""; const badge=document.getElementById("student-badge"); if(badge) badge.textContent=isTeacherMode(s)?`Docente: ${s.studentName||"tmilgar"}`:(s.studentName?`Estudiante: ${s.studentName}`:"Sin identificar"); }
function saveName(){ const s=loadState(); s.studentName=document.getElementById("student-name").value.trim(); s.teacherMode=normalizeText(s.studentName)===TEACHER_USER; saveState(s); if(document.body.dataset.page==="home") renderHome(); if(document.body.dataset.phase) renderPhasePage(document.body.dataset.phase); if(document.body.dataset.page==="certificado") renderCertificate(); }
function renderHome(){ renderShell(""); const s=loadState(); const wrap=document.getElementById("phase-grid"); if(!wrap) return; wrap.innerHTML=APP.phases.map(p=>{ const accessible=isAccessible(p.id,s); const passed=!!s.passed[p.id]; const completed=!!s.completed[p.id]; let st=accessible?"Disponible":"Bloqueada"; let cls=accessible?"status-open":"status-lock"; if(completed&&!passed){st="Completada";cls="status-warn"} if(passed){st="Superada";cls="status-ok"} if(isTeacherMode(s)){st="Modo docente";cls="status-open"} return `<div class="card ${!accessible?'locked':''}"><h3>${p.title}</h3><p class="small">${p.summary}</p><p class="${cls}"><strong>${st}</strong></p><a class="btn" href="${p.id}.html">Abrir fase</a></div>`; }).join(""); }
function renderVerification(id){ const box=document.getElementById("verification-box"); if(!box) return; const s=loadState(); if(isTeacherMode(s)){ box.innerHTML=`<div class="notice">Modo docente: acceso sin validación.</div>`; return;} if(s.verified[id]){ box.innerHTML=`<div class="notice">Práctica validada. Ya puedes acceder al test.</div>`; return;} box.innerHTML=`<div class="panel"><h3>Validación docente</h3><p>Introduce el código facilitado por el profesorado.</p><div class="row"><div class="grow"><input type="password" id="verify-input" placeholder="Código MMDDYY"></div><div><button onclick="submitValidation('${id}')">Validar práctica</button></div></div><p id="verify-msg" class="small"></p></div>`; }
function submitValidation(id){ const s=loadState(); const val=normalizeText(document.getElementById("verify-input").value); const msg=document.getElementById("verify-msg"); if(val===normalizeText(getTodayCode())){ s.verified[id]=true; saveState(s); renderPhasePage(id); } else msg.textContent="Código incorrecto."; }
function nextPhaseButton(id){ const idx=phaseIndex(id); return idx<APP.phases.length-1?`<a class="btn good" href="${APP.phases[idx+1].id}.html">Ir a la siguiente fase</a>`:`<a class="btn good" href="certificado.html">Ir al certificado</a>`; }
function renderTest(id){ const box=document.getElementById("test-box"); if(!box) return; const s=loadState(); const questions=APP.tests[id]||[]; if(isTeacherMode(s)){ box.innerHTML=`<div class="panel"><h3>Vista docente del test</h3><p class="small">Respuestas correctas marcadas.</p>${questions.map((q,i)=>`<div class="quiz-q"><p><strong>${i+1}. ${q.q}</strong></p>${q.options.map((op,idx)=>`<div class="option ${idx===q.answer?'correct-option':''}">${String.fromCharCode(65+idx)}. ${op}${idx===q.answer?' ✓':''}</div>`).join("")}</div>`).join("")}${nextPhaseButton(id)}</div>`; return; } if(!s.verified[id]){ box.innerHTML=`<div class="panel"><h3>Test bloqueado</h3><p>Necesitas validación docente para realizar el test.</p></div>`; return; } if(s.completed[id]){ const r=s.results[id]||{}; box.innerHTML=`<div class="panel"><h3>Resultado registrado</h3><p><strong>${r.status||'Completada'}</strong> · ${r.percent||0}%</p>${nextPhaseButton(id)}</div>`; return;} const used=(s.attempts[id]||[]).length; box.innerHTML=`<div class="panel"><h3>Test de la fase</h3><p class="small">Intentos disponibles: ${Math.max(0,MAX_ATTEMPTS-used)} de ${MAX_ATTEMPTS}</p><form id="quiz-form">${questions.map((q,i)=>`<div class="quiz-q"><p><strong>${i+1}. ${q.q}</strong></p>${q.options.map((op,idx)=>`<label class="option"><input type="radio" name="q${i}" value="${idx}"> ${op}</label>`).join("")}</div>`).join("")}<button type="button" onclick="submitTest('${id}')">Enviar test</button></form></div>`; }
function submitTest(id){ const questions=APP.tests[id]||[]; const data=new FormData(document.getElementById("quiz-form")); let score=0; questions.forEach((q,i)=>{ if(Number(data.get('q'+i))===q.answer) score++; }); const s=loadState(); s.attempts[id] ||= []; s.attempts[id].push(score); s.best[id]=Math.max(s.best[id]||0, score); s.percent[id]=Math.round((s.best[id]/questions.length)*100); if(score>=PASS_SCORE){ s.passed[id]=true; s.completed[id]=true; s.results[id]={status:"Superado",passed:true,score:s.best[id],percent:s.percent[id]}; } else if(s.attempts[id].length>=MAX_ATTEMPTS){ s.passed[id]=false; s.completed[id]=true; s.results[id]={status:"No superado",passed:false,score:s.best[id],percent:s.percent[id]}; } else { s.results[id]={status:"Pendiente",passed:false,score:s.best[id],percent:s.percent[id]}; } saveState(s); renderPhasePage(id); }
function renderSummary(id){ const box=document.getElementById("summary-box"); if(!box) return; const s=loadState(); const r=s.results[id]; box.innerHTML=r?`<div class="panel"><h3>Resumen</h3><p><strong>${r.status}</strong> · Mejor resultado: ${r.score}/${(APP.tests[id]||[]).length} (${r.percent}%)</p>${s.completed[id]?nextPhaseButton(id):''}</div>`:""; }
function savePhase4Brand(){ const s=loadState(); s.phase4Brand=document.getElementById("brand-choice").value.trim(); saveState(s); document.getElementById("brand-msg").textContent="Marca guardada para la fase 5."; const bp=document.getElementById("brand-preview"); if(bp) bp.textContent=s.phase4Brand || "sin guardar todavía"; }
function renderPhase4Brand(){ const el=document.getElementById("brand-choice"); if(!el) return; const s=loadState(); el.value=s.phase4Brand||""; const bp=document.getElementById("brand-preview"); if(bp) bp.textContent=s.phase4Brand||"sin guardar todavía"; const p5=document.getElementById("phase5-brand"); if(p5) p5.textContent=s.phase4Brand || "la marca elegida en la fase 4"; }
function renderPhasePage(id){ renderShell(id); renderVerification(id); renderTest(id); renderSummary(id); renderPhase4Brand(); }
function renderCertificate(){ renderShell(""); const s=loadState(); const box=document.getElementById("certificate-box"); if(!box) return; if(!allCompleted(s)){ box.innerHTML=`<div class="notice">El certificado se desbloquea al completar todas las fases.</div>`; return;} box.innerHTML=`<p><strong>Alumno/a:</strong> ${s.studentName||'Sin identificar'}</p><table><thead><tr><th>Fase</th><th>Resultado</th><th>Porcentaje</th></tr></thead><tbody>${APP.phases.map(p=>{ const r=s.results[p.id]||{}; const st=isTeacherMode(s)?'Revisión docente':(r.status || (s.completed[p.id]?'Completada':'Sin completar')); const pc=isTeacherMode(s)?'—':((r.percent??0)+'%'); return `<tr><td>${p.title}</td><td>${st}</td><td>${pc}</td></tr>`; }).join("")}</tbody></table>`; }
document.addEventListener("DOMContentLoaded",()=>{ if(document.body.dataset.page==="home") renderHome(); if(document.body.dataset.page==="certificado") renderCertificate(); if(document.body.dataset.phase) renderPhasePage(document.body.dataset.phase); });
